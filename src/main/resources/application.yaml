spring:
  application:
    name: service-assistant-backend
  datasource:
    url: jdbc:mysql://localhost:3306/service_center
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
server:
  port: 8080
  error:
    include-message: always